[package]
name = "parse-obsidian"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
comrak = "0.49"
chrono = { version = "0.4", features = ["serde"] }
ignore = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_yml = "0.0"
